import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: { main: "#1b5e20" }, // hijau (sesuai dominan kamu)
  },
  shape: { borderRadius: 12 },
});
